# 审计台 ROADMAP

> 维护规则：新功能先记在这里，被真实数据/真实痛点逼出来了再动手。加功能前先想清楚：这是"新器官"还是"给现有器官上保险"。

## 进行中

（无）

## 待办（按优先级）

### P1 · 杠杆/合约支持 —— 🔒 冻结中，跑满 1 个月现货再解冻

**冻结令（2026-08-21 立）**：现货真实胜率未验证前，不加杠杆。先跑满 1 个月真实数据（追踪账本 ≥30 笔平仓记录），再回来解冻。

**解冻后要做的字段**（提示词 + 审计台 + 合同 JSON 三处同步加）：
- `leverage`（杠杆倍数）→ 仓位计算方式完全不同，需按保证金算
- `margin`（保证金）→ 开仓实际占用
- `liquidation`（强平价）→ 必须反算爆仓距离
- `funding_rate`（资金费率）→ 永续持有成本

**新增审计规则**：
- 爆仓距离 vs 止损距离：止损必须远在强平价之前触发，否则止损形同虚设
- 杠杆下真实盈亏比：用保证金 + 杠杆重算，不能直接用现货公式
- 杠杆上限：默认禁止，除非显式批准（对齐提示词 R-06 的"禁止杠杆"红线，解冻后改为"受控允许"）

**为什么冻结**：当前现货连真实胜率都还没验证（追踪账本无数据），上杠杆 = 在不知道系统准不准的情况下加倍下注。

---

### P2 · 半自动批跑脚本（免费，不花 API 钱）

背景：网页版订阅已付费，API 按 token 另收费，直接上全自动脚本 = 额外花钱买省事。

做法：
- 脚本只做"整理"，不做"调用"：把「提示词 + 当日行情」拼成一段可直接复制的文本
- 用户逐个粘到 5 个网页模型 → 回来把结果粘回审计台
- 省一半复制粘贴，零成本

### P3 · 辩论轻量版（多空对撕）

抄 TradingAgents 的 bull/bear researcher 机制——**一个模型内部前后辩论**：

对分歧最大的 1 个标的（或每次最有争议的标的）：
1. 该模型先当 🐂 多头律师：出 3 条做多论点 + 目标价
2. 再当 🐻 空头律师：**看到多头的论点后**，逐条针对性反驳（不许各说各的）
3. 回到多头：看到空头反驳后，再反驳或承认"这条我无法反驳"
4. 最后当交易员裁决：综合对撕过程出方向

关键区别：模型必须"看到对方论点再反驳"，这是 TradingAgents 的核心，不是各写一份再拼起来。

落地形态：在提示词里加"辩论轮次"指令（模型自己完成多轮），或单独一次调用做对撕。不动 5 模型架构。

---

## 已下线/明确不做

- **行情价格存 GitHub Actions**（每 5 分钟存一次会爆仓库，一年 1GB）→ 砍掉，价格用完即弃
- **IndexedDB / serverless / 多用户** → 单人模拟盘，localStorage 够用，永不引入

## 记录

- 2026-08-21：立杠杆冻结令（跑满 1 个月现货后解冻）
- 2026-08-21：导出备份 + 30 天提醒上线（commit `89144f2`）
- 2026-08-21：审计日志链防篡改上线（commit `f589f74`）
- 2026-08-21：回测账本（胜率/盈亏因子）上线（commit `e81b4d3`）
